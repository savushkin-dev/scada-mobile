# SCADA Mobile PWA

Полнофункциональное Progressive Web Application для мобильного управления SCADA системами.

## 🚀 Возможности

- ✅ Полностью работающее PWA приложение
- ✅ Работает оффлайн (Service Worker)
- ✅ Устанавливается на домашний экран
- ✅ Адаптивный дизайн для всех устройств
- ✅ Современный минималистичный интерфейс
- ✅ Готово к упаковке в TWA (Trusted Web Activity)

## 📁 Структура проекта

```text
/pwa-app
│── index.html                    # Главная страница приложения
│── manifest.webmanifest          # Манифест PWA
│── service-worker.js             # Service Worker для оффлайн режима
│── README.md                     # Документация
│── /assets
│     └── /icons                  # Иконки приложения
│         ├── icon-48x48.png
│         ├── icon-96x96.png
│         ├── icon-128x128.png
│         ├── icon-192x192.png
│         └── icon-512x512.png
│── /css
│     └── styles.css              # Стили приложения
│── /js
      └── app.js                  # Логика приложения
```

## 🛠️ Установка и запуск

### Локальный запуск

1. Установите любой локальный веб-сервер:

```bash
# Используя Python
python -m http.server 8000

# Используя Node.js (npx)
npx http-server -p 8000

# Используя PHP
php -S localhost:8000
```

2. Откройте браузер и перейдите по адресу:

```text
http://localhost:8000/pwa-app/
```

### Запуск с Live Server (VS Code)

1. Установите расширение "Live Server" в VS Code
2. Откройте папку проекта
3. Правой кнопкой мыши на `index.html` → "Open with Live Server"

## 📱 Установка PWA

### На Android

1. Откройте приложение в Chrome
2. Нажмите на меню (три точки)
3. Выберите "Добавить на главный экран"
4. Подтвердите установку

### На iOS

1. Откройте приложение в Safari
2. Нажмите кнопку "Поделиться"
3. Выберите "На экран Домой"
4. Подтвердите добавление

### На Desktop

1. Откройте приложение в Chrome/Edge
2. Нажмите на иконку установки в адресной строке
3. Или используйте меню → "Установить приложение"

## 🔧 Технические детали

### Service Worker

Приложение использует стратегию **cache-first** для максимальной производительности:

- При установке кэшируются все критические ресурсы
- При запросе сначала проверяется кэш
- При отсутствии в кэше - запрос к сети
- Новые ресурсы автоматически добавляются в кэш

### Манифест

Файл `manifest.webmanifest` содержит:

- Название и описание приложения
- Иконки всех необходимых размеров
- Настройки отображения (standalone mode)
- Цветовую схему

### Оффлайн режим

Приложение полностью работоспособно без подключения к интернету благодаря Service Worker.

## 🎨 Кастомизация

### Цвета

Изменить цветовую схему можно в файле `css/styles.css`:

```css
:root {
    --primary-color: #2196F3;    /* Основной цвет */
    --primary-dark: #1976D2;     /* Темный оттенок */
    --secondary-color: #4CAF50;  /* Вторичный цвет */
    ...
}
```

### Иконки

Для создания новых иконок используйте любой графический редактор и сохраните в размерах:

- 48x48px
- 96x96px
- 128x128px
- 192x192px
- 512x512px

### Контент

Отредактируйте `index.html` для изменения содержимого страницы.

## 📦 Упаковка в TWA

Для создания Android приложения:

1. Используйте [Bubblewrap](https://github.com/GoogleChromeLabs/bubblewrap)
2. Или [PWABuilder](https://www.pwabuilder.com/)

```bash
npx @bubblewrap/cli init --manifest=manifest.webmanifest
npx @bubblewrap/cli build
```

## 🔍 Тестирование PWA

### Chrome DevTools

1. Откройте DevTools (F12)
2. Перейдите на вкладку "Application"
3. Проверьте:
   - Manifest
   - Service Workers
   - Cache Storage

### Lighthouse

1. Откройте DevTools (F12)
2. Перейдите на вкладку "Lighthouse"
3. Запустите аудит PWA
4. Цель: 100 баллов по всем метрикам PWA

## 📄 Лицензия

Проект создан для внутреннего использования в рамках SCADA Mobile системы.

## 🤝 Поддержка

При возникновении вопросов или проблем создайте issue в репозитории проекта.
