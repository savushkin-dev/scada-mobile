# ⚡ Быстрый старт: Автоматический деплой PWA

## 🎯 За 30 секунд

### Как это работает

```
Вы делаете изменения в pwa-app/
           ↓
       git push
           ↓
   GitHub Actions запускается
           ↓
 Сайт деплоится на GitHub Pages
           ↓
https://savushkin-dev.github.io/scada-mobile/ обновляется
```

### Готово к использованию

Workflow уже настроен и находится в `.github/workflows/deploy-pwa.yml`.

**Просто делайте commit и push** — остальное сделается автоматически!

---

## 🚀 Деплой в 3 шага

### Шаг 1: Внесите изменения

```bash
# Отредактируйте файлы в папке pwa-app/
# Например:
vim pwa-app/index.html
# или
code pwa-app/
```

### Шаг 2: Коммитьте

```bash
git add pwa-app/
git commit -m "Update PWA UI"
```

### Шаг 3: Push

```bash
git push origin main
```

**✅ Готово!** Через 2-3 минуты сайт обновится на GitHub Pages.

---

## 📊 Мониторинг деплоя

### В браузере

1. Откройте **GitHub** → ваш репозиторий
2. Вкладка **Actions**
3. Найдите **Deploy PWA to GitHub Pages**
4. Смотрите прогресс в реальном времени

### Успешный деплой

```
✓ Prepare → Completed
✓ Build → Completed
✓ Deploy → Completed

PWA successfully deployed to GitHub Pages!
https://savushkin-dev.github.io/scada-mobile/
```

### Ошибка деплоя

```
✗ Prepare → Failed

ERROR: Required file missing: pwa-app/index.html
```

Проверьте структуру `pwa-app/` и попробуйте снова.

---

## 🔍 Проверка после деплоя

### Откройте сайт

```
https://savushkin-dev.github.io/scada-mobile/
```

### Проверьте Digital Asset Links

```
https://savushkin-dev.github.io/scada-mobile/.well-known/assetlinks.json
```

Должен вернуть JSON с вашим SHA-256 отпечатком.

---

## ⚙️ Что делает workflow

| Этап | Что проверяется | Действие |
|------|-----------------|---------|
| **Prepare** | Наличие файлов в `pwa-app/` | Валидирует структуру и JSON |
| **Build** | Наличие `package.json` | Устанавливает зависимости (если есть) |
| **Deploy** | Готовность к публикации | Загружает на GitHub Pages |

---

## 🐛 Частые проблемы

### ❌ Workflow не запускается

**Решение:** Workflow срабатывает только при изменении `pwa-app/` или самого workflow.

```bash
# Внесите какое-то изменение в pwa-app/
echo "<!-- updated -->" >> pwa-app/index.html

# Коммитьте и пушьте
git add pwa-app/index.html
git commit -m "Trigger workflow"
git push origin main
```

### ❌ GitHub Pages показывает 404

**Решение:** Ожидайте 1-2 минуты, кэш может быть старым.

```bash
# Откройте в инкогнито (Ctrl+Shift+N) или очистите кэш
# Ctrl+Shift+Delete
```

### ❌ Ошибка "Required file missing"

**Решение:** Проверьте структуру локально.

```bash
cd pwa-app
ls -la

# Должны быть:
# index.html
# manifest.webmanifest
# service-worker.js
# .well-known/assetlinks.json
```

---

## 📚 Подробная документация

Для полного разбора всех деталей:

👉 [`GITHUB_ACTIONS_GUIDE.md`](./GITHUB_ACTIONS_GUIDE.md)

---

## 💡 Советы

### Ускорить деплой локально

```bash
# Перед push, протестируйте локально
cd pwa-app
npm run dev

# http://localhost:8000/

# Когда всё готово:
cd ..
git add pwa-app/
git commit -m "..."
git push origin main
```

### Откатить деплой

```bash
# Если нужно откатить последний коммит
git revert HEAD
git push origin main

# Или удалить коммит
git reset --hard HEAD~1
git push -f origin main
```

### Просмотреть логи деплоя

```bash
# GitHub Actions → Deploy PWA to GitHub Pages → последний запуск
# → Expand любой из этапов (Prepare, Build, Deploy)
```

---

## 🎉 Готово

Ваш PWA теперь деплоится автоматически! 🚀

**Любые изменения в `pwa-app/` → push → автоматический деплой!**
