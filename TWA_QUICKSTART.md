# 🔥 Быстрый старт TWA

## 🚨 ВАЖНО: Почему видна полоска сверху?

**Короткий ответ:** Потому что вы используете `http://127.0.0.1:8000/` — локальный адрес разработки.

**Digital Asset Links работают ТОЛЬКО с:**

- ✅ HTTPS
- ✅ Публичными доменами (например: `https://your-app.netlify.app`)

**НЕ работают с:**

- ❌ HTTP
- ❌ `127.0.0.1`, `localhost`, `192.168.x.x`

## 📱 Текущая ситуация (Development)

### Что происходит сейчас

```
Ваше Android приложение → пытается проверить
http://127.0.0.1:8000/.well-known/assetlinks.json
                        ↓
                Android НЕ МОЖЕТ проверить (локальный адрес)
                        ↓
                Верификация провалена
                        ↓
        Chrome показывает полоску (Custom Tabs режим)
```

### Это НОРМАЛЬНО для локальной разработки! ✅

Полоска сверху — **ожидаемое поведение** при работе с локальным сервером.

## 🎯 Как убрать полоску? (2 варианта)

### Вариант 1: Деплой на HTTPS (Production) 🚀

**Шаги:**

1. **Деплой PWA на хостинг с HTTPS:**

   ```bash
   # Netlify (рекомендуется)
   # 1. Зарегистрируйтесь на netlify.com
   # 2. Drag & Drop папку pwa-app
   # 3. Получите URL: https://your-app.netlify.app
   ```

2. **Обновить AndroidManifest.xml:**

   ```xml
   <!-- Заменить: -->
   android:value="http://127.0.0.1:8000/"
   <!-- На: -->
   android:value="https://your-app.netlify.app/"
   
   <!-- И включить: -->
   <intent-filter android:autoVerify="true">
   ```

3. **Обновить MainActivity.kt:**

   ```kotlin
   val twaUrl = "https://your-app.netlify.app/"
   ```

4. **Пересобрать приложение:**

   ```bash
   cd twa-mobile
   ./gradlew installDebug
   ```

5. **Готово!** Полоска исчезнет через несколько минут (или после переустановки).

### Вариант 2: Продолжить работу локально (с полоской) 🛠️

**Для разработки и тестирования это приемлемо:**

```bash
# 1. Запустить PWA сервер
cd pwa-app
npm run dev

# 2. Запустить Android приложение
cd ../twa-mobile
./gradlew installDebug

# 3. Тестировать с полоской сверху (функционал работает полностью)
```

**Плюсы:**

- ✅ Быстрая итерация
- ✅ Не нужен интернет
- ✅ Весь функционал работает

**Минусы:**

- ⚠️ Полоска сверху видна
- ⚠️ Не полноценный TWA режим

## 📋 Текущая конфигурация (уже исправлена)

### ✅ Что я уже сделал

1. **AndroidManifest.xml** — обновлён:
   - Порт изменён с `5500` → `8000` (совпадает с `npm run dev`)
   - Убран `autoVerify` для локальной работы
   - Добавлены комментарии для production

2. **MainActivity.kt** — обновлён:
   - URL изменён на `http://127.0.0.1:8000/`
   - Добавлены комментарии для production

3. **assetlinks.json** — корректный:
   - SHA-256 совпадает с вашим debug keystore
   - Package name правильный
   - Готов к работе после деплоя

### 🔍 Проверка текущей конфигурации

```bash
# Проверить, что сервер запущен на правильном порту
cd pwa-app
npm run dev
# Должен показать: http://localhost:8000/

# Проверить package name в Android
grep "package=" twa-mobile/app/src/main/AndroidManifest.xml
# Должно быть: com.savushkin.scada.mobile

# Проверить SHA-256
cd twa-mobile
./gradlew signingReport
# Найдите: A5:42:03:8F:8F:29:DD:B8:C3:BF:CA:3C:9F:21:1D:9B:F4:82:13:18:A8:94:DB:EE:56:3F:25:D1:07:3E:2D:E4
```

## 🚀 Рекомендация

### Для разработки

**Продолжайте работать локально** с полоской — это быстрее и удобнее.

```bash
# Запустить всё одной командой (из корня проекта)
cd pwa-app && npm run dev
```

### Для демонстрации/презентации

**Сделайте быстрый деплой на Netlify:**

1. Перетащите папку `pwa-app` на [netlify.com/drop](https://app.netlify.com/drop)
2. Получите URL (например: `https://scada-mobile-xyz.netlify.app`)
3. Обновите 2 файла (AndroidManifest.xml + MainActivity.kt)
4. Пересоберите: `./gradlew installDebug`
5. Полоска исчезнет! ✨

## 📚 Подробная документация

Полное руководство по деплою: [`TWA_DEPLOYMENT_GUIDE.md`](./TWA_DEPLOYMENT_GUIDE.md)

## ❓ FAQ

### Q: Можно ли как-то убрать полоску локально?

**A:** Нет. Digital Asset Links — это система безопасности Android. Обход невозможен и не нужен.

### Q: Сколько времени нужно на деплой?

**A:**

- Netlify/Vercel: ~5 минут
- GitHub Pages: ~10 минут
- Обновить 2 файла в Android: ~2 минуты
- Пересобрать: ~30 секунд

**Итого:** ~15-20 минут для полноценного TWA.

### Q: Нужно ли платить за хостинг?

**A:** Нет! Бесплатные варианты:

- ✅ Netlify (бесплатно до 100GB/месяц)
- ✅ Vercel (бесплатно для личных проектов)
- ✅ GitHub Pages (бесплатно для публичных репозиториев)

### Q: Что если у меня несколько разработчиков?

**A:** Добавьте SHA-256 каждого разработчика в `assetlinks.json`:

```json
{
  "sha256_cert_fingerprints": [
    "A5:42:03:...:E4",  // Debug keystore разработчика 1
    "B6:53:14:...:F5",  // Debug keystore разработчика 2
    "C7:64:25:...:G6"   // Release keystore для production
  ]
}
```

## 🎉 Итог

1. **Локально:** Полоска видна — это нормально ✅
2. **Production:** После деплоя на HTTPS полоска исчезнет ✅
3. **Ваша конфигурация:** Уже готова и правильная ✅
4. **Следующий шаг:** Деплой на Netlify/Vercel (15 минут) 🚀

---

**Нужна помощь?** Смотрите подробное руководство: [`TWA_DEPLOYMENT_GUIDE.md`](./TWA_DEPLOYMENT_GUIDE.md)
