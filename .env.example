# Spring profile for backend container
SPRING_PROFILES_ACTIVE=prod

# If PrintSrv runs on host machine, keep host.docker.internal
# If PrintSrv runs elsewhere, set real IP/hostname
PRINTSRV_IP=host.docker.internal
PRINTSRV_PORT=10101

# Optional JVM options
JAVA_OPTS=-Xms256m -Xmx512m

# CORS allowlist for browser clients (fallback/direct backend calls)
CORS_POLICY_ALLOWED_ORIGINS=http://localhost:8081,http://127.0.0.1:8081
