/**
 * Публичный barrel конфигурации frontend.
 *
 * Правило использования:
 * - компоненты импортируют константы из этого файла, если не нужен
 *   узкий модульный импорт;
 * - при поиске «источника правды» переходите в конкретный модуль
 *   (`runtime`, `domain`, `ui`, `styles`).
 */
export * from './runtime';
export * from './domain';
export * from './ui';
export * from './styles';
