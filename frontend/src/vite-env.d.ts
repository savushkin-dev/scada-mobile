/**
 * Ambient-типы Vite для фронтенда.
 *
 * Здесь намеренно нет доменной логики: файл подключает типы окружения
 * (import.meta.env, HMR и т.п.) для всего проекта.
 * Схема и валидация env-переменных находятся в {@link ./schemas/env.ts}.
 */
/// <reference types="vite/client" />
