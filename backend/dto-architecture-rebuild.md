# 📐 DTO → Domain Refactoring Plan

## SCADA Mobile Backend — Архитектурное выпрямление

---

## 🎯 Цель

Перевести проект от смешанной DTO-архитектуры к чётко разделённой структуре с:

* Domain Model (ядро системы)
* PrintSrv Protocol Layer (интеграция)
* REST API Layer (публичный контракт)
* Infrastructure (хранилища, сокеты, конфигурация)

Главная задача — разорвать связность между слоями и обеспечить устойчивость к изменениям.

---

# 🚨 Текущая проблема

DTO используются одновременно как:

* модель протокола PrintSrv
* внутренняя модель состояния
* модель для хранения snapshot
* модель для REST API
* модель для mock-ответов

### Последствия

1. Tight coupling между REST и PrintSrv
2. Nullable-поля без гарантий инвариантов
3. Семантическая перегрузка DTO
4. Нарушение Single Responsibility
5. Сложность тестирования бизнес-логики

---

# 🧠 Архитектурный принцип

DTO — это форма передачи данных.
Domain Model — это смысловая модель системы.

Domain Model не знает:

* про Spring
* про JSON
* про REST
* про сокеты
* про PrintSrv DTO

DTO — не должны использоваться внутри бизнес-логики.

---

# 📁 Итоговая целевая структура

```
backend/
│
├── api/                         # REST Layer (public contract)
│   ├── controller/
│   └── dto/
│
├── printsrv/                    # Integration Layer
│   ├── client/
│   └── dto/
│
├── domain/                      # Core Business Logic
│   ├── model/
│   └── service/
│
├── application/                 # Orchestration layer (use-cases)
│
├── store/                       # Infrastructure storage
│
├── config/
└── exception/
```

---

# 🏗 Domain Layer (ядро системы)

## domain/model/

### Создать:

* `UnitSnapshot`
* `DeviceSnapshot`
* `WriteCommand`
* `CommandBuffer` (если логика буферизации — часть бизнес-механики)

### Требования:

* immutable объекты
* non-null поля (если логически обязательны)
* инварианты проверяются в конструкторе
* отсутствие аннотаций Spring/Jackson

Domain модели отражают реальность системы, а не формат передачи.

---

# 🔌 PrintSrv Layer

## printsrv/dto/

Переместить:

* QueryAllRequestDTO
* QueryAllResponseDTO
* SetUnitVarsRequestDTO
* SetUnitVarsResponseDTO
* ParametersDTO
* UnitsDTO
* PropertiesDTO

Это внешний контракт интеграции.

Изменение PrintSrv протокола не должно ломать domain.

---

# 🌐 API Layer

## api/dto/

Создать:

* QueryStateResponseDTO
* ChangeCommandRequestDTO
* ChangeCommandResponseDTO
* ErrorResponseDTO

API DTO отражают контракт с клиентом.

API может отличаться от PrintSrv протокола.

---

# 🔄 Mapping Strategy

Создать два маппера:

### 1️⃣ PrintSrvMapper

```
PrintSrv DTO → Domain Model
```

Используется в polling / интеграционном слое.

### 2️⃣ ApiMapper

```
Domain Model → API DTO
```

Используется в контроллерах.

Мапперы:

* не содержат бизнес-логики
* только преобразование структуры

---

# 🧩 Buffer & Store

## CommandBuffer

Если содержит:

* логику агрегации
* дедупликацию
* тайминги
* правила отправки

→ должен быть частью domain.

Если просто хранит список —
→ может остаться в store.

---

## SnapshotStore

Остаётся в store.

Он:

* хранит состояние
* не принимает решений
* не содержит бизнес-логики

---

# 🏛 Service Layer Разделение

## Domain Service

Работает только с domain моделями.
Не знает про REST и PrintSrv.

## Application Service

Оркестрирует:

* domain
* printsrv client
* store

Это координационный слой.

---

# 📋 План миграции

## Фаза 1 — Создание структуры

* создать `domain/model`
* создать `api/dto`
* создать `printsrv/dto`

---

## Фаза 2 — Domain Model

* реализовать `WriteCommand`
* реализовать `UnitSnapshot`
* реализовать `DeviceSnapshot`
* переписать buffer на использование `WriteCommand`

---

## Фаза 3 — PrintSrv Separation

* переместить DTO в `printsrv/dto`
* написать PrintSrvMapper
* перевести polling на domain модели

---

## Фаза 4 — API Separation

* создать API DTO
* написать ApiMapper
* обновить контроллеры

---

## Фаза 5 — Cleanup

* удалить использование PrintSrv DTO вне integration слоя
* убрать nullable-поля из domain
* удалить устаревшие DTO

---

# 🛡 Инварианты архитектуры

После завершения:

1. Domain не зависит ни от одного внешнего слоя.
2. REST можно менять независимо от PrintSrv.
3. PrintSrv протокол можно менять независимо от API.
4. Store хранит domain объекты, а не DTO.
5. Business logic не зависит от Spring.

---

# 📈 Критерии прогресса

✔ Domain модели существуют
✔ DTO разделены по bounded contexts
✔ Нет использования PrintSrv DTO в контроллерах
✔ Нет использования API DTO в polling
✔ Buffer работает с WriteCommand
✔ Snapshot хранится как Domain Model
✔ Мапперы реализованы
✔ Nullable устранены в domain

---

# 🎯 Итоговое состояние системы

Система становится:

* устойчивой к изменениям
* расширяемой
* тестируемой
* предсказуемой
* архитектурно зрелой

Это переход от “код работает”
к “система спроектирована”.

---

# 🧭 Стратегическая цель

Архитектура должна позволять:

* изменить стратегию буферизации
* изменить протокол PrintSrv
* изменить REST контракт
* добавить новые команды

— без каскадных правок по всему проекту.

Если это возможно — архитектура реализована правильно.
