-- Снятие ограничения уникальности с названия устройства в справочнике.
-- Основание: у двух разных по коду устройств может быть одинаковое название;
-- уникальность остаётся только на code (uc_device_catalog_code, V7).
-- Идемпотентно: безопасно применять повторно.

ALTER TABLE device_catalog DROP CONSTRAINT IF EXISTS uc_device_catalog_name;
