-- Значение CurItem (текущая партия/изделие), зафиксированное при активации
-- уведомления «последняя партия». Показывается получателям в тексте уведомления
-- и в истории задач. NULL для старых записей и при недоступности значения.
ALTER TABLE production_notifications
    ADD COLUMN cur_item VARCHAR(255);
