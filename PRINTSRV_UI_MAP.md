# Карта соответствий: старая SCADA ↔ PrintSrv ↔ новый фронт

Источники истины:
- реальные логи PrintSrv (`printsrv-logs`, 24.09.2026 10:07–10:32, 87 опросов на устройство, машины: Bosch, Grunwald5, Hassia1–6);
- скриншоты старой SCADA и нового фронта в один момент (24.09.2026 ~8:05–9:00);
- mock-снимки `backend/src/main/resources/mock-snapshots/` (grunwald11, trepko1/2 — реальных логов по ним НЕТ).

Документ фиксирует, откуда старая SCADA берёт отображаемые данные, что сейчас показывает новый фронт, и где неуверенность.

## 1. Устройства и нумерация `scada`-тегов

Агрегатор `scada` публикует диагностику с префиксами `Dev0XX` (камеры) и `LineDev0XX` (принтеры). Старая SCADA называет устройства прямо по этим номерам: «Камера 41» = `Dev041`, «Принтер 1» = `Printer11` = `LineDev011`.

| Объект старой SCADA | scada-префикс | Физическое устройство PrintSrv | Уверенность |
|---|---|---|---|
| Камера 41 (Поток 1) | `Dev041` | `CamAgregation` / `CamAgregation1` | ДОКАЗАНО счётчиками (см. §2) |
| Камера 42 (Поток 1) | `Dev042` | `CamAgregationBox` / `CamAgregationBox1` | высокая (карта Grunw11 + порядок LineElementID); прямого доказательства нет — `Dev042CounterGeneral` в логах всегда 0 |
| Камера 43 (Поток 2) | `Dev043` | `CamAgregation2` | высокая (Grunwald11: счётчики К43 есть, mock содержит Dev043*; правило 4(2k−1)) |
| Камера 44 (Поток 2) | `Dev044` | `CamAgregationBox2` | высокая (по аналогии с Dev042) |
| Камера 71–74 | `Dev071`–`Dev074` | `CamEanChecker1`–`CamEanChecker4` | ДОКАЗАНО: `Dev071CounterGeneral`=1223 == `CamEanChecker1.Succeeded`=1223 (и точно так же 072/073/074, Grunwald5) |
| Камера 3 (Trepko, Поток) | `Dev03` | `CamChecker` (Trepko) | СРЕДНЯЯ — реальных логов Trepko нет; выведено из скриншота Trepko11 (К3 со счётчиками, строки CMSDev03) |
| Принтер 1/2/3/4 | `LineDev011`–`LineDev014` | `Printer11`–`Printer14` | ДОКАЗАНО (строки «ConnectionPrinterNN ST» ↔ `LineDev0NNConnection`) |
| «Принтер 2» в группе «Поток» (Hassia4) | `LineDev02` | `Printer2` | ДОКАЗАНО: старая SCADA «ConnectionPrinter2 ST=1» == `LineDev02Connection=1` в логе |
| — (нет в старой SCADA) | нет Dev-префикса | `CamChecker` / `CamChecker1/2` | высокая: свободных номеров Dev04x под него нет, старая SCADA его нигде не показывает (см. §3) |
| — (нет в старой SCADA) | ? | `CamBatch`, `CamPacker`, `CamPackerBox` (Trepko) | НЕИЗВЕСТНО — нет логов Trepko, в UI старой SCADA их нет |

Правило нумерации потоков: поток k → `Dev04(2k−1)` (камера агрегации) + `Dev04(2k)` (box-камера). Поток 1 = 041/042, поток 2 = 043/044.

## 2. Счётчики «Считано/Несчитано»

### Старая SCADA

Источник — теневые счётчики агрегатора `scada`:

| Поле UI | Тег | Что это |
|---|---|---|
| Считано | `Dev0XXCounterGeneral` | теневой счётчик scada-модуля, ≈ `CamAgregation.Succeeded` + дрейф 0..+87 |
| Несчитано | `Dev0XXCounterMissing` | ≈ `CamAgregation.Failed` ± малый дрейф |

Доказательства (логи ↔ скриншоты):
- Hassia5 (камера заморожена с 08:07): старая SCADA **8200/0** == `Dev041CounterGeneral`=8200, `Dev041CounterMissing`=0 — ТОЧНО. При этом `CamAgregation.Succeeded`=8185 ≠ 8200 → старая SCADA читает НЕ поле устройства, а именно тег `scada`.
- Hassia1: `Dev041CounterGeneral`=40196 == `CamAgregation.Succeeded`=40196 (CamChecker.Succeeded=40130 — мимо).
- CounterMissing == `CamAgregation.Failed`: Hassia6 34==34, Grunwald5 7==7, Hassia2 24==24 (у CamChecker.Failed в это время 1993/1895/505 — категорически мимо).
- Bosch: старая SCADA 46488/24 ≈ `CamAgregation.Succeeded`=46477 (+11, типичный дрейф) / `Failed`=24 (точно). В логе 10:07 `Dev041CounterGeneral`=0 — теневые счётчики scada-модуля были сброшены между 8:11 и 10:07 (см. §5).

Кому показываются счётчики: ТОЛЬКО первой камере каждой «потоковой» группы (К41 для Поток 1, К43 для Поток 2 на Grunwald11; К3 для Поток и К41 для Агрегация на Trepko11). Вторым камерам (К42/К44) и камерам К71–74 счётчики НЕ показываются (строки пустые), хотя `Dev042*`/`Dev071*` теги в `scada` есть и ненулевые (у EAN). Принтерам счётчики не показываются никогда.

### Новый фронт (текущее, неверное)

- Карточка `CamChecker` (группа checkerCams, без scada-префикса) показывает `CamChecker.Total` / `CamChecker.Failed`. Доказано: Bosch мобильный **46537/26** == замороженные `Total`=46537/`Failed`=26 точно.
- `Total` = `Succeeded` + `Failed` → «Считано» систематически ЗАВЫШЕНО на величину `Failed` (плюс дрейф CounterGeneral). Это и есть замеченное «в моём фронте больше».
- На Grunwald5 `CamChecker.Failed` огромен (тысячи) → расхождение тысячи; на Hassia/Bosch `Failed` мал → «ближе всего у CamChecker», как и заметил автор.
- `CamAgregation` (та камера, чьи счётчики показывает старая SCADA) в БД-топологии реальных автоматов ОТСУТСТВУЕТ → карточки с правильными счётчиками нет вообще. Авто-discovery по `LineDevices` должен её добавить.
- EAN-камеры показываются со счётчиками — старая SCADA их не показывает.

### Целевое правило

1. «Считано/Несчитано» = `Dev0XXCounterGeneral`/`Dev0XXCounterMissing` из `scada` (как сейчас — приоритет верный).
2. Fallback при отсутствии тега: `Succeeded` (НЕ `Total`!) для «Считано», `Failed` для «Несчитано». Отступление от ТЗ_исправление_runtime_счетчиков_и_ошибок.md (там `Total`) — по данным логов `CounterGeneral` ≈ `Succeeded`, а не `Total`.
3. Счётчики показывать только устройствам с признаком `showCounters` (per-unit конфиг); дефолт — камерам агрегации (К41/К43/…), остальным — нет.

## 3. Группировка устройств в старой SCADA

Таблица устройств старой SCADA делится на группы-строки:

| Машина | Группа «<Имя машины>» | «Поток» / «Поток 1» | «Поток 2» | «Агрегация» |
|---|---|---|---|---|
| Bosch | Принтер 1, Принтер 2 | Камера 41*, Камера 42 | — | — |
| Grunwald 5 | Камера 71–74, Принтер 1, Принтер 2 | Камера 41*, Камера 42 | — | — |
| Grunwald 11 | Камера 71–74, Принтер 1–4 | Камера 41*, Камера 42 | Камера 43*, Камера 44 | — |
| Hassia 1/2 | Принтер 1 | Камера 41*, Камера 42 | — | — |
| Hassia 3/5/6 | Принтер 1, Принтер 2 | Камера 41*, Камера 42 | — | — |
| Hassia 4 | Принтер 1 (11), Принтер 2 (12) | **Принтер 2 (Printer2)**, Камера 41*, Камера 42 | — | — |
| Trepko 11 | Принтер 1 | Камера 3* | — | Камера 41*, Камера 42 |

`*` = камера со счётчиками.

Выводы:
- `CamChecker` (проверка DM сразу после печати) в таблице старой SCADA НЕ отображается нигде. Его ошибки в таблице ошибок тоже не представлены отдельно (нет Dev-префикса).
- EAN-камеры (К71–74) сидят в группе машины, без счётчиков; в таблице ошибок у них только 2 строки: «Нет связи с устройством», «Несовпадение ean13…».
- Принтер может сидеть в «Потоке» (Hassia4, `Printer2`) → состав групп НЕ выводится из PrintSrv полностью; нужен per-unit конфиг в админке. Дефолт предлагается такой: принтеры и EAN-камеры → группа машины; камеры агрегации/box → «Поток N» (N из scada-префикса); `CamChecker` → «Поток» без счётчиков (или скрыт).
- Визуально «Поток» и «автомат» на старой SCADA — равноправные строки-группы одной таблицы; группа с именем машины идёт первой.

## 4. Ошибки

Таблица «Ошибки устройств» старой SCADA = все флаги `scada` по устройствам (строка на флаг, значение справа; ненулевое = активно):

| Суффикс тега | Текст старой SCADA | Текущий текст backend |
|---|---|---|
| `Batch` | Не совпадает идентификатор партии | совпадает |
| `Connection` | Нет связи с устройством | совпадает |
| `DiffEan` | Несовпадение ean13 в коде упаковки и … | близко («Несовпадение EAN-13 в коде упаковки и идентификаторе партии») |
| `Dublicate` | Одинаковые коды маркировки | совпадает |
| `Fail` | Нет кодов маркировки | совпадает |
| `Data` | Нет данных с устройства | совпадает |
| `Work` | (тот же текст «Нет данных с устройства») | совпадает |
| `Error` | общий флаг-rollup, отдельной строкой/событием не показывается | показывается «Общая ошибка устройства» — ЛИШНИЙ дубль в журнале |
| принтеры: `LineDev0NNConnection` | строка «ConnectionPrinterNN ST»; событие журнала «Принтер N — Нет подключения!» | «Нет связи с устройством» у `LineDevNNN` |

Группа в журнале старой SCADA — человекочитаемая: «Камера 41», «Поток 2. Камера 43.», «Принтер 1», «Маркировка» (системные события: «Ошибка выполнения команды», «Такая партия уже добавлена в очередь» — источник: `Line.Error`/`ErrorMessage`/команды BatchQueue).

Проблемы текущего фронта: журнал показывает сырые `objectName` (`Dev041`, `LineDev02`) и сырой `propertyDesc` (`LineDev02Error`); активный `Error=1` даёт вторую, дублирующую запись рядом с конкретным флагом (Hassia4: `LineDev02Error` + `LineDev02Connection`, тогда как старая SCADA показывает только «Нет подключения!»).

Целевое правило: имя устройства в журнале/алерте = per-unit display name (с префиксом группы, если групп несколько: «Поток 2. Камера 43»); сырой ключ сохранять в `propertyDesc`; флаг `Error` подавлять, если у того же устройства активен любой конкретный флаг (Connection/Fail/Dublicate/DiffEan/Work/Data/Batch).

## 5. Жизненный цикл счётчиков (важно для ожиданий)

- Смена партии счётчики НЕ сбрасывает (Grunwald5: 32679→32703 при смене 327→328).
- Полный сброс наблюдался между 8:09 и 10:07 при ТОЙ ЖЕ партии: упали и теневые (`Dev041CounterGeneral` 32703→13583), и device-счётчики (`CamChecker.Total` 37355→15163). Вероятная причина — рестарт PrintSrv/scada-модуля. Точная причина НЕ подтверждена — на паритет отображения не влияет: показываем тот же тег, что и старая SCADA.
- `DevTotal`, `BatchSucceeded/BatchTotal` и прочие поля камер для UI не нужны.

## 6. Прочие подтверждённые источники

- Строка партии у принтера/линии (`3852 | 63 | 24.09.2026`) = `curitem` устройства / `Line.CurItem`.
- Состав устройств линии = `Line.LineDevices`; роли на смену партии = `Level1Printers/Level2Printers/Level1Cams/Level2Cams` — к ГРУППИРОВКЕ UI прямого отношения не имеют (проверено: Hassia4 `Level1Cams=CamChecker,CamAgregation`, но камеры в «Потоке», а не в группе машины).
- `scada.lineerr` = общий флаг ошибки линии (1 при активных ошибках устройств).

## 7. Открытые вопросы (помечены, реализацию не блокируют — решаются конфигом)

1. Trepko: `Dev03` = `CamChecker` — средняя уверенность; `CamBatch`/`CamPacker`/`CamPackerBox` не замаплены — нужны реальные логи Trepko11.
2. `Dev042` = `CamAgregationBox` — высокая уверенность, но без прямого числового доказательства (счётчики всегда 0).
3. Причина полного сброса счётчиков (рестарт?) — неизвестна.
4. Набор error-строк по типам устройств в старой SCADA конфигурочен (у CMSDev071–074 только 2 строки, у CMSDev03 нет Batch) — у нас единый набор суффиксов; при желании 1:1-paritet нужен per-type конфиг текстов — пока не делаем.
5. Дрейф `CounterGeneral` vs `Succeeded` (0..+87) — внутренняя особенность scada-модуля, не ошибка; для паритета читаем `CounterGeneral`, а не пересчитываем.
